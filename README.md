# ninjutsu-build

A TypeScript library for creating ninja files (https://ninja-build.org/).

# Setup

  1. `cd packages/core`
  2. `npm install`
  3. `cd ../../tests`
  4. `npm install`

# Building

  1. `cd packages/core`
  2. `npm run build`
  3. `cd ../../tests`
  4. `npm run test`

